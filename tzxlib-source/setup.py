from setuptools import setup, find_packages

setup(
    name='tzxlib',
    version='1.0',  # You can change the version number
    packages=find_packages(),
    install_requires=[],  # Add any dependencies here if needed
)